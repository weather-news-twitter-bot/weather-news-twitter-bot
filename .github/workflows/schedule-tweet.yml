name: 📅 Weather News Bot (Initial Post)

on:
  schedule:
    # 毎日 JST 18:00 (UTC 09:00) に実行
    - cron: '0 9 * * *'
  workflow_dispatch: # 手動実行を許可
    inputs:
      mode: # ★ 復活させた入力
        description: '対象日の決定モード (auto: 18時以降は翌日)'
        required: true
        default: 'auto'
        type: choice
        options:
        - auto
        - today
        - tomorrow

jobs:
  post_schedule:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    permissions:
      contents: write
      
    steps:
    - name: Checkout repository (with full history)
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # -------------------------------------------------------------
    # 1. Pythonの環境構築
    # -------------------------------------------------------------
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies and Playwright Browsers
      run: |
        pip install -r requirements.txt
        playwright install chromium
        playwright install-deps

    # -------------------------------------------------------------
    # 2. 初回投稿ロジックの実行 (データ生成 & ツイート)
    # -------------------------------------------------------------
    - name: 📢 Run Initial Post Bot
      env:
        # Twitter API Keys (GitHub Secrets)
        TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
        TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        # Bot Settings
        EXECUTION_MODE: 'post'
        # ★ 修正箇所: 手動入力 'mode' を環境変数に渡す
        SCHEDULE_TARGET_MODE: ${{ github.event.inputs.mode }}
        TZ: 'Asia/Tokyo'
      run: python src/weather_bot.py
      
    # -------------------------------------------------------------
    # 3. 状態ファイルのコミットとプッシュ 
    # -------------------------------------------------------------
    - name: 💾 Commit and Push Schedule Data to Repo
      if: success()
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "BOT: Initial post schedule data commit"
        file_pattern: schedule_data.json
